version: '3'
services:
        web:
                image: dvass_port_v1 
                build: .
                ports:
                        - "8452:8452"  
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
